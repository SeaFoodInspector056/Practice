#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    vector<vector<char>> maze(10, vector<char>(10, '.'));

    string line;
    while (getline(cin, line))
        
    












    return 0;
}