#include <bits/stdc++.h>
using namespace std;

bool solve()
{
    vector<vector<int>> vec(6, vector<int>(6));
//這題她媽太難了
    

}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;
    while (n--)
        cout << (solve() ? "correct" : "incorrect") << '\n';

    return 0;
}